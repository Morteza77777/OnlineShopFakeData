<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Exam" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:statusBarColor">@color/orange</item>
        <item name="backgroundColor">@color/back_app</item>
        <item name="android:fontFamily">@font/sans_fa_name_regular</item>
    </style>

    <style name="AppTheme.FullScreen" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="MaterialButtonShape">
        <item name="cornerSize">25dp</item>
        <item name="android:gravity">center</item>
    </style>

    <style name="Theme.Exam" parent="Base.Theme.Exam" />

</resources>